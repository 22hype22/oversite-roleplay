# TTS plays NATIVELY (discord.py voice + FFmpeg) with locally synthesized audio
# — the architecture of the reference TTS bot (no external audio server for
# TTS). Music still uses the shared Lavalink node. Native voice needs the
# ffmpeg binary (opus encoding happens in ffmpeg) and PyNaCl (in requirements).
[phases.setup]
nixPkgs = ["...", "ffmpeg"]
